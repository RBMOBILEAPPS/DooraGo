# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Enable AndroidX — required because Flutter and all SDK dependencies use AndroidX artifacts
android.useAndroidX=true
# Jetifier disabled — all dependencies are already native AndroidX; enabling it causes
# Java heap space OOM when Jetifier attempts to scan Flutter's large native engine JARs.
android.enableJetifier=false
